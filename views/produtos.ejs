<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
    rel="stylesheet" />
    <link rel="icon" type="image/png" href="/imgs/Logo.png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
    xintegrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
      
  <link rel="stylesheet" href="css/produtos.css" />
  <title>RotuScan - Guia Completo sobre Intolerância alimentares</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet" />
</head>

<body>
  <div class="loading-screen" id="loadingScreen">
    <div class="loading-spinner"></div>
  </div>

  <div class="scroll-progress" id="scrollProgress"></div>

  <%- include('./partials/navbar.ejs') %>

  <section id="home" class="hero">
   <div class="textLeft">
      <p class="Slogan">RotuScan o app<br>
        que te ajuda a se<br>
        alimentar melhor</p>
      <p>Quer se alimentar sem medo? Decidir rápido no mercado? Ajudar quem você ama?</p>
      <a href="#" class="btnBaixe">Baixe já!</a>
    </div>

    <div>
      <img src="imgs/produtos/header.png" class="ImgHome">
    </div>
  </section>

  <div class="product">
    <header class="page-header">
      <h1>Produtos</h1>
      <p class="subtitle">
        Sua fonte de consulta segura. Encontre qualquer produto do nosso catálogo e veja sua análise nutricional
        completa.
        Garantindo que você saiba exatamente o que está levando para casa.
      </p>
    </header>

    <div class="products-grid">
      <% if (!produtos || produtos.length === 0) { %>
        <p class="text-center">Nenhum produto cadastrado.</p>
      <% } else { %>
        <% produtos.forEach(produto => { %>
          <a href="/produto/<%= produto.id %>" class="product-card-link">
            <div class="product-card">
              <div class="product-image">
                <img src="<%= produto.url_imagem_produto %>" alt="<%= produto.nome_gen %>">
              </div>
              <div class="product-info">
                <span class="product-category">
                  Cadastrado por: <%= produto.nomeCadastrador %>
                </span>

                <h3 class="product-name">
                  <%= produto.nome_gen %>
                </h3>

                <span class="product-badge">
                  <%= produto.marca_produto %>
                </span>

                <!-- ---------- MOSTRAR APENAS INGREDIENTES RESUMIDOS ---------- -->
                <% if (produto.ingredientes && produto.ingredientes.length) { %>
                  <div class="mt-2">
                    <% produto.ingredientes.slice(0,3).forEach(function(ing){ %>
                      <span class="badge bg-light text-dark me-1 mb-1" title="<%= ing.descricao || '' %>">
                        <i class="fas fa-seedling"></i> <%= ing.nome %>
                      </span>
                    <% }) %>
                    <% if (produto.ingredientes.length > 3) { %>
                      <small class="text-muted">+<%= produto.ingredientes.length - 3 %> mais</small>
                    <% } %>
                  </div>
                <% } %>

              </div>
            </div>
          </a>
        <% }); %>
      <% } %>
    </div>

    <a href="/addProdutos" class="floating-action-button" title="Cadastrar Novo Produto">
      <i class="fas fa-plus"></i>
    </a>
</body>
</html>